function buildCustomWidgets() {
  // Initialize custom/additional widgets in this method like the example below

  // Notice that you need to be sure that your widget script and css are either
  // loaded in the main searchView.jsp (in case you are using widget class
  // already used by Datafari) or in the customWidgets.jsp (for widgets not
  // loaded by default by Datafari)

  // Manager.addWidget(new AjaxFranceLabs.TableWidget({
  // elm : $('#facet_extension'),
  // id : 'facet_extension',
  // field : 'extension',
  // name : window.i18n.msgStore['facetextension'],
  // pagination : true,
  // selectionType : 'OR',
  // returnUnselectedFacetValues : true
  // }));
}