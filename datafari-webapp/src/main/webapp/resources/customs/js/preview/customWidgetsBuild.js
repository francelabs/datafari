function buildCustomWidgets(manager) {
  // Initialize custom/additional widgets in this method like the example below

  // Notice that you need to be sure that your widget script and css are either
  // loaded in the main previewUI.jsp (in case you are using widget class
  // already used by Datafari) or in the customWidgets.jsp (for widgets not
  // loaded by default by Datafari)

  // manager.addWidget(new AjaxFranceLabs.PreviewNavigationWidget({
  // elm : $("#preview-nav"),
  // id : "preview-nav",
  // name : window.i18n.msgStore['preview-navigation']
  // }));
}